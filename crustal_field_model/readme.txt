The *_real files are SH coeffs from the longitudinally averaged field intensity of a SH magnetic model. They have the default parameters of SHCoeffs classes.

The other/older files correspond to the field intensity of a longitudinally averaged SH magnetic model and therefore underestimate the real values. These files have the default parameters of SHMagCoeffs classes.